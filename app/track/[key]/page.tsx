"use client";
import { useEffect, useState } from "react";
const time = (v: number | null) => v ? new Intl.DateTimeFormat("en-IN", { hour: "numeric", minute: "2-digit" }).format(v) : "Updating";
export default function TrackingPage({ params }: { params: Promise<{ key: string }> }) { const [key, setKey] = useState<string>(); const [visit, setVisit] = useState<{ token: string; status: string; etaLower: number | null; etaUpper: number | null; recommendedArrival: number | null; doctor: string }>(); useEffect(() => { void params.then(value => setKey(value.key)); }, [params]); useEffect(() => { if (!key) return; const load = () => fetch(`/api/track/${key}`).then(r => r.ok ? r.json() : undefined).then(setVisit); void load(); const id = window.setInterval(load, 3000); return () => window.clearInterval(id); }, [key]); if (!visit) return <main className="tracking"><main><p className="eyebrow">Clinicify</p><h1>Loading your secure visit…</h1></main></main>; return <main className="tracking"><header><a className="brand" href="/"><span>✚</span> CLINICIFY</a><button>Live queue tracking</button></header><main><p className="eyebrow">Your OPD visit</p><h1>{visit.token}</h1><p className="tracking-doctor">{visit.doctor}</p><div className="track-status"><span className="pulse"/> QUEUE STATUS · {visit.status.replace("_", " ")}</div><section className="eta-card"><p>Expected consultation</p><h2>{time(visit.etaLower)} – {time(visit.etaUpper)}</h2><span>Recommended arrival <b>{time(visit.recommendedArrival)}</b></span></section><aside className="priority-note"><b>Queue updates live</b><p>A priority case may adjust your time, but no other patient information is ever shown.</p></aside><p className="last-updated">Automatically checks for queue updates every few seconds.</p></main></main>; }
